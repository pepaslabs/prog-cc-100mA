enum _code_t
{
  UNINITIALIZED=0,

  COMMAND_INCREMENT=1,
  COMMAND_DECREMENT=2,
  COMMAND_SET_CURRENT=3,
  COMMAND_SET_CODE=4,
  COMMAND_SET_CODE_W_GAIN=5,
  COMMAND_CALIBRATE_FULL_SCALE=6,
  COMMAND_CALIBRATE_ZERO=7,
  COMMAND_DUMP=8,
  COMMAND_DISABLE_VERBOSE_DEBUG_OUTPUT=9,
  COMMAND_ENABLE_VERBOSE_DEBUG_OUTPUT=10,
  
  END_OF_COMMANDS_SECTION=11,

  OK_NO_ERROR=20,
  
  ERROR_UNKNOWN_COMMAND=30,
  ERROR_BUFFER_FILLED_UP_BEFORE_SENTINEL_REACHED=31,

  ERROR_INCREMENT_WOULD_CAUSE_OVERFLOW=40,
  ERROR_DECREMENT_WOULD_CAUSE_UNDERFLOW=41,

  ERROR_PARSED_CURRENT_OUTSIDE_SUPPORTED_RANGE=50,
  ERROR_PARSED_CODE_OUTSIDE_SUPPORTED_RANGE=51,
  
  ERROR_PARSED_GAIN_OUTSIDE_SUPPORTED_RANGE=60,
  ERROR_PARSED_VREF_OUTSIDE_SUPPORTED_RANGE=61
};

typedef enum _code_t command_t;
typedef enum _code_t error_t;

